---
description: Add a versioned workflow node or lifecycle capability safely.
---
Read `AGENTS.md`, ADR 0001, and the OpenAPI/AsyncAPI contracts. Update the strict workflow schema, graph diagnostics, compiler, checkpointed runtime, run events, React Flow canvas, accessible outline, inspector, and tests together. Published versions remain immutable, run retries remain idempotent, external effects fail closed, and every drag action needs a button or keyboard alternative.
